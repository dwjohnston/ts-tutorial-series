{
  "name": "lesson-1-compilation",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib", 
    "build": "yarn clean && tsc",
    "build:1": "yarn clean && tsc -p tsconfig.1.json", 
    "start:1": "node lib/index1.js",
    "build:2": "yarn clean && tsc  -p tsconfig.2.json", 
    "start:2": "node lib/index2.js"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
